import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4881D6',
    },
    secondary: {
      main: '#D67224',
    },
  },
});

export default theme;